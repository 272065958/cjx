<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cjx_background_color"
    android:orientation="vertical">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar_view" />

    <android.support.design.widget.TabLayout xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/tab_layout"
        style="@style/tablayout_style"
        app:tabIndicatorColor="@color/cjx_colorPrimary"
        app:tabMode="scrollable"
        app:tabSelectedTextColor="@color/cjx_colorPrimary"
        app:tabTextAppearance="@style/my_tab_apperance" />

    <include
        layout="@layout/item_list_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>